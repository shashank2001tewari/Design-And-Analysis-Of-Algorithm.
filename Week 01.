include<iostream>
using namespace std;
int binary(int a[],int k,int n)
{
    Int flag=0;
for(int i=0;i<n;i++)
{
  if(a[i]==k)
{
flag=1;
}
return flag;
}
int main()
{
    int t;
    int a[100];
    int n;
    int k;
           int flag=0;
    cin>>t;
    while(t)
    {
      cin>>n;
      for(int i=0;i<n;i++)
      {
          cin>>a[i];
      }
      cin>>k;
      flag=linear(a,k,n);
      if(flag)
      {
          cout<<k<<”Is present.”;
      }
      else
      {
          cout<<"\n Key not found in the array:::::";
      }
      t--;
    }
    return 0;
}
